/**
 * Format constants for 1v1 and 2v2 leagues.
 * These correspond to the id values in the formats table.
 * Safe to import from both client and server code.
 */
export const FORMAT_1V1 = 1;
export const FORMAT_2V2 = 2;
